# Task 3 report: AES-GCM, S3 evidence, retention, and readiness

## Scope delivered

- Added Node-compatible AES-256-GCM evidence encryption with detached 12-byte IV and 16-byte authentication tag.
- Added asynchronous boto3 S3/MinIO storage with 3 total bounded attempts for retryable transport/408/429/5xx failures, no retries for authorization or validation errors, short connect/read timeouts, encrypted-object content type enforcement, and safe object keys.
- Added transactional MariaDB evidence metadata/audit repository and an evidence service that uploads encrypted bytes before metadata, compensates by deleting an uploaded object after metadata failure, provides scoped 60-second content tokens, safe content headers, audit events, and retention purge ordering.
- Added `/health/ready` for database, storage, and the Task 5-replaceable queue readiness protocol; failures contain availability states only.
- Added `proctoring-check-infra` and `proctoring-purge`; operational dependencies `boto3`, `cryptography`, and `jinja2` now ship in base dependencies. Imports/runtime assembly do not issue DB or S3 requests.

## Files

- `apps/api/src/proctoring_api/services/evidence_crypto.py`
- `apps/api/src/proctoring_api/services/object_storage.py`
- `apps/api/src/proctoring_api/services/evidence.py`
- `apps/api/src/proctoring_api/services/readiness.py`
- `apps/api/src/proctoring_api/repositories/evidence.py`
- `apps/api/src/proctoring_api/cli.py`
- `apps/api/src/proctoring_api/app.py`
- `apps/api/src/proctoring_api/config.py`
- `apps/api/src/proctoring_api/main.py`
- `apps/api/pyproject.toml`
- `apps/api/tests/test_evidence.py`
- `apps/api/tests/test_evidence_integration.py`

## TDD evidence

RED command, before the new crypto adapter existed:

```text
> .venv\Scripts\python.exe -m pytest apps/api/tests/test_evidence.py -q
ERROR apps/api/tests/test_evidence.py
ModuleNotFoundError: No module named 'proctoring_api.services.evidence_crypto'
1 error in 0.30s
```

The command test was also RED before its sanitizing handler:

```text
> .venv\Scripts\python.exe -m pytest apps/api/tests/test_evidence.py::test_infrastructure_command_has_a_sanitized_nonzero_failure_exit -q
FAILED apps/api/tests/test_evidence.py::test_infrastructure_command_has_a_sanitized_nonzero_failure_exit
RuntimeError: S3 secret-key must not be printed
1 failed in 2.17s
```

GREEN verification:

```text
> .venv\Scripts\python.exe -m compileall apps/api/src -q
> .venv\Scripts\python.exe -m pytest apps/api/tests -q
112 passed, 1 skipped in 3.18s
> .venv\Scripts\proctoring-check-infra.exe
Infrastructure unavailable
command_exit=1
```

The command was intentionally run without application configuration. It exits nonzero with a sanitized response and no traceback.

## Fixture provenance and compatibility

`NODE_AES_256_GCM_FIXTURE` is a literal AES-256-GCM NIST SP 800-38D known-answer vector: 32 zero-byte key, 12 zero-byte IV, 16 zero-byte plaintext, ciphertext `cea7403d4d606b6e074ec5d3baf39d18`, and tag `d0d1c8a799996bf0265b98b5d48ab919`. It is not generated by the implementation under test.

The frozen Node adapter `apps/api/src/services/evidence-encryption-service.js` uses `createCipheriv('aes-256-gcm', key, iv)` without AAD and writes ciphertext, IV, and `getAuthTag()` separately. The Python adapter preserves those three fields exactly and round-trips a Python-generated wire object in a separate test. Historical migrations `004_evidence_panel.sql` and `006_incident_evidence.sql` store `encryption_iv` and `encryption_tag` but no key-version field; the Python adapter therefore preserves the historical key-version metadata as absent (`None`) rather than inventing incompatible database metadata.

## SHAs

- Task 3 code and tests: `f57edefe395a755b39771693de1e15ff1d09f4f1`
- Parent baseline: `4fcdfac82825a5735ed58f3007c939a9564ed954`

## Gated concerns

- `apps/api/tests/test_evidence_integration.py` is marked `integration` and skipped unless `TEST_DATABASE_URL` plus `TEST_S3_ENDPOINT`, `TEST_S3_BUCKET`, `TEST_S3_ACCESS_KEY_ID`, and `TEST_S3_SECRET_ACCESS_KEY` are present. No live MariaDB/S3 integration is claimed here.
- Node/npm/pnpm were not run. The existing Node source and SQL fixtures were read only as frozen compatibility references.
- Task 5 owns the durable queue; Task 3 deliberately supplies only the `QueueReadiness` interface seam.
